import React, { Component } from 'react';
import {
    SafeAreaView,
    Text,
} from 'react-native';

export default class TestModal extends Component {
    render() {
        return (
            <SafeAreaView>
                <Text>testModal</Text>
            </SafeAreaView>
        );
    }
}